# ingest.yaml — raw evidence → chunks
# cp configs/ingest.yaml.example configs/ingest.yaml

raw_dirs:
  - data/raw/statements
  - data/raw/forensics
  - data/raw/messenger
  - data/raw/logs
  - data/raw/corporate_card
  - data/raw/network

output:
  chunks: data/processed/chunks.jsonl
  summary: runs/ingest/summary.yaml

chunking:
  max_chars: 500
  overlap: 50
  # messenger/jsonl 은 줄 경계 청킹(ingest.py)으로 evidence_id 절단 방지
